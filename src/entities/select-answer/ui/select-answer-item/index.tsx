import { ISelectAnswer } from "../../model/select-answer";

export interface SelectAnswerItemProps {
  answer: ISelectAnswer;
}
export const SelectAnswerItem = ({ answer }: SelectAnswerItemProps) => {
  return <div></div>;
};
